@model VirtualStore.Ingredient
@{
    ViewBag.Title = "Ingredients";
}
<style>
    .dataTeble {
        margin-top: 50px;
    }

    input {
        margin-bottom: 50px;
        margin-top: 50px;
    }
</style>
<h2>Ingredients</h2>
<form method="post">
    <input type="submit" name="action" value="Add" />
    <input type="submit" name="action" value="Change" />
    <input type="submit" name="action" value="Delete" />
    <input type="submit" name="action" value="Update" />
    <br>
    <table class="dataTeble">
        @*@ViewBag.DataList*@
        @*@Html.TextBox("DataList")*@
    </table>
    @using VirtualStore;
    @using VirtualStorePlace.LogicInterface;
    @using VirtualStorePlace.ConnectingModel;
    @using VirtualStorePlace.UserViewModel;
    <select name="ingredientsList" multiple>

        @{ int i = 0;
            foreach (IngredientUserViewModel st in @ViewBag.DataList)
            {
                i++;
                <option value="@i">@st.Id @st.IngredientName</option>
            }
        }
    </select>
</form>
